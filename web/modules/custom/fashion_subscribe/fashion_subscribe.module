<?php

/**
 * Implements hook_cron().
 */
function fashion_subscribe_cron() {
    // We access our configuration.
    $cron_config = \Drupal::configFactory()
        ->getEditable('fashion_subscribe.settings');
    $state = \Drupal::state();
    $users = $state->get('users');
}